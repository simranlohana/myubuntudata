---
  - hosts: mhost1
    gather_facts: False
    vars_files:
            - vars_file.yml


    tasks: 
            - name: Displaying variable
              debug:
                      msg: "{{ users }}"
