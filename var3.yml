---
  - hosts: mhost1
    gather_facts: False
    vars:
            list:
                    - item1
                    - item2
                    - item3
    tasks:
            - name: Displaying variable
              debug:
                      msg: "{{ list[2] }}"
